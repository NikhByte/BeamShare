# Critical Project Rules: Jules v2 (Jitro) Usage

- ALWAYS use the new Jules v2 (Jitro) API / CLI (`/home/nikhil/.local/bin/jules`), NEVER use the legacy Jules v1 alpha session tools (`create_session` MCP tool) to avoid wasting credits.
- The user has unlimited usage access to Jules v2 (Jitro).
- The Jules workspace ID for BeamShare is `8fdf6848-6cd4-4aa1-9182-58024e5d82e4`.
- Use `jules` CLI commands (`jules goals`, `jules tasks`, `jules insights`, `jules assessments`) to manage, inspect, and execute tasks.
- Keep verifying test suites (`go test -race ./...` and `npm test`) and merging pull requests / branches into `main`.
